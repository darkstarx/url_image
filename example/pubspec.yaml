name: example
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  file_storage: ^1.0.0
  flutter:
    sdk: flutter
  flutter_svg: ^2.0.17
  http: ^1.3.0
  logging: ^1.3.0
  url_image:
    path: ../

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
